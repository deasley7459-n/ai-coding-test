根目录作为全栈示例项目的顶层容器，将后端（backend/）与前端（frontend/）两个独立子模块并列组织：后端基于 Spring Boot 提供 /api/users 系列 REST 接口并开启 CORS；前端通过 Vite 开发服务器将 /api 请求代理到 localhost:8080，同时 Axios 调用也依赖后端的跨域配置，从而在本地调试时实现前后端解耦运行、在生产构建时可分别部署。根级 README.md 约定了统一的启动顺序（先后端再前端）、端口分配（8080/5173）以及统一的响应契约 `{code, message, data}`，是连接两个子模块的协作契约。